#pragma once
#include <cstddef>

// This is a FROM-SCRATCH problem — see problem.md for the required public
// interface. There is no skeleton here on purpose: design the class
// yourself (member layout, helper methods, everything) as long as it
// exposes exactly the constructor/get/put/size signatures described.
//
// You'll likely want: <unordered_map>, <list> (or your own doubly linked
// structure), <utility>.

template <typename K, typename V>
class LRUCache {
    // TODO: implement from scratch per problem.md
};
